[user]
	email = jlavoie1602@gmail.com
	name = Justin Lavoie
[color]
	branch = auto
	diff = auto
	interactive = auto
	status = auto
	decorate = auto
	ui = auto
	showbranch = auto
	grep = auto

[color "branch"]
	current = green bold
	locate = yellow
	remote = blue bold

[color "status"]
	added = green bold
	changed = blue bold
	untracked = red bold

[color "diff"]
	meta = blue bold
	frag = magenta bold
	old = red strike
	new = green italic

[core]
	editor = "nvim"
	pager = "less"
	excludesfile = ~/.gitignore
[alias]
	cm = commit -v
	cma = commit -av
	st = status -sb
	co = checkout
	br = branch -av
	ps = push
	pu = pull --rebase
    lg = log --graph --oneline --all --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%aN>%Creset' 
	di = diff
	re = remote -v
[github]
	user = just1602
[push]
	default = current
[diff "gpg"]
	textconv = gpg --no-tty --decrypt
