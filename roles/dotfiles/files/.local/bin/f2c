#!/usr/bin/env python

import sys

if len(sys.argv) != 2:
    print(f'{sys.argv[0]} <fahrenheit temperature>')
    sys.exit(-1)

fahrenheit = float(sys.argv[1])
celsius = (fahrenheit - 32) / 1.8

print(f'{celsius:.2f}')
